{
  "name": "qvesta-website",
  "compatibility_date": "2026-09-18",
  "build": {
    "command": "node scripts/check-catalog-env.mjs production && npm run build && npm run test:build -- production"
  },
  "assets": { "directory": "./dist/client", "not_found_handling": "single-page-application" },
  "env": {
    "staging": {
      "name": "qvesta-website-stage",
      "build": {
        "command": "node scripts/check-catalog-env.mjs staging && npm run build:stage && npm run test:build -- staging"
      },
      "routes": [{ "pattern": "stage-www.qvesta.ru", "custom_domain": true }],
      "assets": { "directory": "./dist/client", "not_found_handling": "single-page-application" }
    }
  }
}
