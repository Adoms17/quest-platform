# OPS-01.4 — подготовка выпуска на stage

Дата: 23.09.2026. Статус: foundation, уведомление об отказе и ограничение приёмки одной организацией опубликованы на stage. Автопродление выключено; отдельная тестовая организация создана, серверная область настроена. Production вне области выпуска.

## Состав

17 новых миграций: 20260922100000–20260922230000 (почасовые), 20260922233000, 20260922234500, 20260922235000. Требуется полностью применённое основание OPS-01.3 до 20260922090000 включительно. Проверка истории: scripts/check-admin-stage-migrations.mjs --recurring-release; она только проверяет список и не применяет миграции. Существующие режимы платежного/тарифного выпуска не расширены.

Функции: sandbox-checkout, sandbox-reconcile, yookassa-sandbox-webhook получают обновлённые общие модули; новая sandbox-recurring выполняет подготовку, сверку и выдачу. Frontend включает отдельный блок согласия только при VITE_SANDBOX_RECURRING_SETUP=true. Новая функция требует одновременно YOOKASSA_SANDBOX_ENABLED=true и YOOKASSA_SANDBOX_RECURRING_ENABLED=true, а также серверный worker-токен. Значения секретов не помещать в отчёт или Git.

## Порядок выпуска с выключенным функционалом

1. Проверить diff и состав коммита; зафиксировать SHA кандидата, CI и историю stage. Не применять все локальные миграции без точного allowlist.
2. Использовать зарегистрированный workflow deploy-staging.yml: сначала operation=recurring-dry-run, затем operation=recurring-apply на том же проверенном SHA ветки staging. Workflow проверяет allowlist до dry-run и повторно перед apply, после apply требует пустой pending-набор. Существующий payments-release по-прежнему отклоняет recurring-миграции. Отдельный recurring-staging.yml недоступен для dispatch без регистрации в default branch; тот же изолированный job перенесён в deploy-staging.yml. Миграции ещё не запускались.
3. Применить миграции штатным Git-процессом после dry-run. Флаги UI/автопродления остаются выключенными, расписание отсутствует.
4. Опубликовать перечисленные функции и frontend того же SHA. Проверить 401 без токена и 503 при выключенном recurring.
5. Только после закрытия условий ниже включить тестовый интерфейс согласия и провести приёмку на отдельной sandbox-организации. Запуск recurring вручную ограниченным worker-вызовом, без расписания.

## Условия до включения

- Политика утверждена Алексеем 23.09.2026: после окончательного отказа показать уведомление и предложить ручную оплату, без повторных автоматических списаний. Неизвестный результат продолжать сверять без нового платежа. Закрытый заказ не возобновляется автоматически.
- Текст согласия и статусы обновлены локально: sandbox-recurring-v2 явно разрешает сохранение метода и тестовые списания. Сервер отклоняет v1. Визуальная проверка отдельного компонента с синтетическим API на 390/1280 px пройдена; нужны интеграционная приёмка checkout и новое явное согласие в sandbox; сохранение согласия не означает включённого серверного флага. Изменена ещё не опубликованная foundation-миграция; если обнаружится среда с применённой прежней редакцией, остановить выпуск и подготовить отдельную миграцию перехода.
- Добавить безопасное уведомление о неудачном продлении и понятный способ ручной оплаты, согласно утверждённой политике.
- Реальный sandbox: сохранение метода через тестовый checkout; сверка метода; продление, скидки 50%/100%, отзыв до отправки, отказ, восстановление после сбоя, отсутствие повторного периода. Не подменять это результатами синтетического провайдера.
- Проверить сохранение отсутствия доступа к методу оплаты в браузере, RLS/MFA и поведение после окончания поддержки версии.

## Отключение и восстановление

При ошибке выключить YOOKASSA_SANDBOX_RECURRING_ENABLED и UI-флаг, остановить расписание, если оно появится. Это прекращает новые вызовы, но не отменяет уже разрешённые/отправленные платежи. Сохранить журналы и резервы; завершить их сверку отдельным контролируемым действием. Не удалять таблицы, не откатывать миграции с данными, не возобновлять закрытые заказы и не создавать новый idempotency key для неизвестного платежа.

## Доказательства

Локально прошли replay 73 миграций, SQL/RLS и конкурентные проверки, 179 тестов серверных модулей, отдельный Edge smoke и единая цепочка Edge/PostgREST/БД с синтетическим провайдером. Проверка allowlist: 11 тестов PASS. Это не подтверждение работы реального провайдера. История прогонов: OPS-01.4-recurring.md.


## Выпуск миграций — 23.09.2026

PR #83 и #84 объединены. Stage SHA: 7e5df1e69276fc19ac9e753eb359ff23a002c788. CI PR #84 PASS. Dry-run 35817705300 подтвердил точные 17 миграций; apply 35817819356 PASS, после применения pending отсутствует. Cloudflare Build приложения для PR #83 PASS, HTTP /organization/billing = 200. Функции, расписания и флаги этим workflow не изменялись. Production не изменён.

## Публикация функций — 23.09.2026

Supabase CLI опубликовал sandbox-recurring, sandbox-checkout, sandbox-reconcile, yookassa-sandbox-webhook в stage jeugfyaqzfgdvfhdxfht. Diff функций/config относительно staging 7e5df1e пустой. Перед публикацией явно установлен YOOKASSA_SANDBOX_RECURRING_ENABLED=false. Проверка endpoint: GET 405, POST без токена 401; оба ответа пустые, Cache-Control=no-store. Проверка 503 с корректным токеном на stage ещё не выполнена; токен не считывался. Расписание не добавлялось, списания не запускались. Следующий шаг: проверка с токеном через GitHub Actions и уведомление об окончательном отказе до включения.

## Проверка выключенного обработчика на stage — 23.09.2026

PR #85 объединён после успешного CI (10m16s) и CodeQL. Stage SHA: 7d4d44cc440ba7138d5b9039fdaed67136b3fd74. Workflow recurring-disabled-smoke, run 35823038229, PASS: GET 405, POST без токена 401, POST с корректным worker-токеном 503. Все ответы пустые, Cache-Control=no-store. Перед проверкой workflow повторно установил YOOKASSA_SANDBOX_RECURRING_ENABLED=false. Секреты не выводились, автопродление не включалось. Осталось реализовать уведомление об окончательном отказе и провести реальную sandbox-приёмку перед включением.

Выпуск уведомления выполняется отдельно: сначала backend PR, recurring-notice-dry-run/apply (только 20260923010000 при применённых предыдущих миграциях), затем отдельная публикация frontend. UI пока остаётся локальным. Guard: 12 тестов PASS.

## Выпуск RPC уведомления — 23.09.2026

PR #86 объединён после всех CI. Stage SHA 1fa9998e73985c9983bc4877fe8218bd5ccefc77. Dry-run 35828436352 подтвердил только 20260923010000; apply 35828536322 PASS, pending отсутствует. UI выпускается отдельным PR после этой миграции; повтор 21 UI/API/page теста PASS. Автопродление не включалось.

## Выпуск интерфейса уведомления — 23.09.2026

PR #87 объединён после CI validate PASS (10m22s) и CodeQL. Stage SHA ca8d68eef87c6caf6e368d6b5ee7457c6673ec41. Cloudflare Workers Builds: quest-platform-staging PASS. HTTP /organization/billing=200; публичный модуль OrganizationBilling-D5wvHGVv.js содержит read_recurring_failure_notice, текст отказа и sandbox-manual-checkout. Это проверка публикации, не реальная sandbox-приёмка отказа/оплаты. Автопродление не включалось, production не изменён.


## Подготовка реальной sandbox-приёмки — 23.09.2026

По коду runSandboxRecurringBatch обработчик вызывает prepare_due_sandbox_recurring(shop), затем list_sandbox_recurring_work(shop). Обе операции ограничены магазином и пачкой до 10, но не одной выбранной организацией. До ручного запуска приёмки требуется серверное ограничение организации одновременно в подготовке и выборе очереди. Фильтрация после чтения очереди недостаточна: подготовка/резервирование уже могли затронуть другие организации.

План следующего среза:
1. Алексей выбрал отдельную тестовую организацию. Подготовить её для приёмки; smoke-organizer не использовать.
2. Добавить отдельную миграцию с безопасным ограничением подготовки/очереди, передаваемым из серверной настройки, не из произвольного тела HTTP. Невалидная настройка должна блокировать работу. Проверить две организации одного магазина: невыбранная не получает заказов, резервов или изменений расписания сверки.
3. Выпустить это ограничение с выключенным флагом и повторить disabled smoke.
4. Включить интерфейс согласия; владелец выбранной организации лично сохраняет v2-согласие и проводит новый checkout тестовой картой. Старые заказы без согласия не преобразовывать в подписку задним числом.
5. Проверить подтверждённое сохранение метода, затем ограниченный ручной запуск без расписания. Отдельно: успешное продление, скидка 50/100%, отзыв, отказ с уведомлением, ручная оплата и повторная сверка без дубликатов. Не сокращать реальные оплаченные периоды/данные организации ради теста; при необходимости подготовить отдельную синтетическую организацию и согласованный тестовый период.
6. После приёмки вернуть recurring=false; записать агрегированные результаты без идентификаторов метода, токенов или платёжных реквизитов.

В этом шаге новые списания и изменения базы не выполнялись; выбрана отдельная тестовая организация, создание ещё не выполнено.

## Ограничение организации для приёмки — локально, 23.09.2026

Добавлена миграция 20260923020000_scope_recurring_acceptance.sql с отдельными service_role RPC prepare_scoped_sandbox_recurring(shop,organization) и list_scoped_sandbox_recurring_work(shop,organization). Фильтр включён в SQL до подготовки/захвата, исходные проверки сохраняются. NULL не расширяет область, browser execute запрещён. Старые RPC оставлены для совместимости; endpoint пока не переключён, поэтому эта миграция сама по себе не ограничивает текущий worker.

Тест двух организаций одного магазина подтверждает отсутствие заказов/резервов у невыбранной и отсутствие изменения её расписания сверки при чтении очереди выбранной. Первый прогон: 9/10 новых проверок, ошибка тестового ожидания очереди из-за now() в одной транзакции; добавлены тестовые попытки без HTTP. Повтор: полный replay 75 миграций, SQL/RLS/concurrency/PostgREST/Edge PASS, 149.95 с. Lint/build PASS с прежними предупреждениями. Stage не менялся, новая миграция не опубликована.

Далее: обязательная валидируемая серверная настройка UUID выбранной организации, переключение endpoint/batch на scoped RPC, тест запрета запуска без настройки, контролируемый выпуск. Отдельная тестовая организация ещё не создана, автопродление выключено.

## Подключение ограничения к обработчику — локально, 23.09.2026

sandbox-recurring читает YOOKASSA_SANDBOX_RECURRING_ORGANIZATION_ID только из серверного окружения. Без валидного UUID enabled=false; HTTP body не задаёт организацию. runSandboxRecurringBatch дополнительно отвергает невалидный UUID до любых RPC и передаёт p_organization_id одинаково в prepare_scoped_sandbox_recurring и list_scoped_sandbox_recurring_work. Тестовый Edge получает UUID своей синтетической организации из fixture.

Проверки: первоначальный unit-тест обнаружил неверное имя аргумента RPC; исправлено на p_organization_id. Полный повтор: 20 тестов в трёх файлах PASS, включая replay 75 миграций и цепочку PostgREST/Edge с синтетическим провайдером, 149.64 с. Lint/build/diff-check PASS (прежние warnings). Изменения локальные, stage не менялся.

Следующее: отдельный guard/workflow для миграции 20260923020000, PR и stage-выпуск с выключенным флагом; публикация нового handler только после миграции. Затем создание отдельной тестовой организации и её явная серверная настройка. Согласие владельца и тестовая оплата остаются обязательными шагами реальной sandbox-приёмки.

## Выпуск ограничения приёмки — 23.09.2026

PR #88 объединён в staging: b8dffcbfa2bf33bae0007573b0fb9748402f1a33. CI validate, admin-frontend и CodeQL PASS. Первый admin-auth-integration завершился ошибкой на этапе браузерного входа; повтор того же кода в run 35837484790 PASS. Причина первого сбоя не установлена, изменение кода ради повтора не выполнялось.

Dry-run 35843617852 подтвердил только 20260923020000_scope_recurring_acceptance.sql. Apply 35843747840 применил её на том же SHA; после применения pending отсутствует. Код функций/config совпадает со staging. Перед публикацией sandbox-recurring явно установлен YOOKASSA_SANDBOX_RECURRING_ENABLED=false. CLI подтвердил публикацию. Disabled smoke 35843998466 PASS: 405/401/503, пустые ответы и no-store. Расписание не добавлялось; списания не запускались.

Следующее: определить владельца отдельной тестовой организации, создать её и настроить серверный UUID области приёмки. Затем включить интерфейс согласия, получить новое явное согласие владельца и тестовую оплату, проверить сохранение метода и ограниченное продление. Реальная sandbox-приёмка ещё не выполнена. До неё автопродление остаётся выключенным.
## Подготовка отдельной организации — 23.09.2026

По подтверждению Алексея создана sandbox-recurring-acceptance на stage. Существующий выбранный аккаунт назначен владельцем через активное членство и штатную роль owner. Создание выполнено транзакционно с блокировкой и проверкой владельца при повторе. Организация добавлена в billing_sandbox_application_scope; отдельный read-back подтвердил организацию и роль. Прежние организации и подписки не изменялись. Изменений схемы нет.

Настроен YOOKASSA_SANDBOX_RECURRING_ORGANIZATION_ID на созданную организацию, YOOKASSA_SANDBOX_RECURRING_ENABLED=false подтверждён успешной командой настройки. Подписка новой организации пока unconfigured. Следующее: начальный Free и персональное sandbox-предложение, интерфейс согласия, затем явное согласие и тестовая оплата владельцем. Заказы, согласия и списания в этом шаге не создавались. Код не менялся; lint/build повторно не запускались.
## Предложение отдельной организации — 23.09.2026

Новой организации назначена актуальная версия Free только из unconfigured. Создано одно персональное sandbox-предложение актуальной версии Pro: 200 копеек, один календарный месяц, срок принятия семь дней. Использован единственный уже настроенный sandbox-магазин без вывода его идентификатора. Повтор подготовки не добавляет предложение. Проверка list_sandbox_checkout_offers под ролью authenticated с контекстом владельца вернула visible_offers=1. Заказ и согласие не создавались, платёж не отправлялся.

Публичный stage bundle OrganizationBilling-D5wvHGVv.js не содержит формы согласия. Следующий шаг: в Cloudflare Builds приложения stage установить VITE_SANDBOX_RECURRING_SETUP=true и пересобрать. Браузерный инструмент дважды не запустился (ошибка среды/ACL), поэтому изменение этой настройки не выполнено. Серверный флаг recurring остаётся false. До появления и сохранения явного согласия не проводить тестовую оплату для этой приёмки. Код не менялся, lint/build не запускались; git diff --check PASS.
## Подтверждение первого checkout с согласием — 23.09.2026

После включения пользователем VITE_SANDBOX_RECURRING_SETUP и пересборки публичный модуль OrganizationBilling-C1lP4lif.js содержит форму, флажок и кнопку сохранения согласия. Пользователь сообщил об оплате. Независимое чтение stage подтвердило для отдельной организации: 200 копеек, succeeded/paid, requires_review=false, заказ finished, fulfillment applied, подписка active. Согласие sandbox-recurring-v2 создано до first_sent_at, не отозвано; verified_method_saved=true. Идентификаторы метода и платежа не считывались.

Период: 23.09.2026 12:51:58 — 23.10.2026 12:51:58 (Москва). Это подтверждает первый реальный sandbox-checkout и сохранение метода, но не повторное списание. Серверное автопродление в этом шаге не включалось. Следующий этап: отдельный контролируемый сценарий наступления продления без сокращения оплаченного периода, затем проверка повторного списания и идемпотентности. Изменён только этот журнал; git diff --check PASS, lint/build не запускались.

## Ускоренная приёмка — подготовка

Алексей явно разрешил досрочно завершить только тестовый период отдельной организации и проверить продление на 2 рубля. До готовности разового запуска даты не меняются. Добавлен recurring-acceptance-run: только staging, обязательный UUID, токен из GitHub Secrets, один HTTP-вызов без retry, always-выключение и disabled smoke. При аварийном прекращении runner выключить флаг отдельно и сверить неизвестный результат без нового платежа. Локальные профильные тесты: 15 PASS; lint PASS с прежними предупреждениями. Реальное повторное списание ещё не выполнено.


## Реальное sandbox-продление — 23.09.2026

PR #89 объединён после CI PASS (10m39s) и CodeQL: staging e8c4f12942ca44252c584822c680c5c50acdb383. Локально 15 тестов, lint, build и diff-check PASS. По отдельному разрешению Алексея period_end только тестовой организации перенесён на текущий момент с проверкой исходной даты и revision=2. Первая проверка с rollback ранее подтвердила один расчёт на 200 копеек без HTTP.

Разовый workflow 35848808095 PASS: processed=1, failed/reconciliationRequired/reviewRequired=0. Независимое чтение БД: один recurring order, одна attempt, одна dispatch, один succeeded/paid результат без review и одно подтверждение периода на 200 копеек. Повтор 35849013594 PASS: processed=0; количество заказов, попыток и подтверждений осталось 1/1/1. Новый период active: 23.09.2026 13:26:10 — 23.10.2026 13:26:10 (Москва), revision=4.

После обоих запусков recurring=false; disabled smoke 405/401/503 PASS. Расписания нет. Production не изменён. Это подтверждает реальное sandbox-продление сохранённым методом и отсутствие дубля при повторном проходе. Следующие сценарии: скидки 50/100%, отзыв до отправки, окончательный отказ и ручное восстановление; они ещё не закрыты.


## Дополнительные проверки БД stage с откатом — 23.09.2026

В двух ограниченных транзакциях (statement_timeout=20s, ROLLBACK) проверена опубликованная логика на отдельной организации. Временные скидки и запись использованного льготного периода были синтетической фикстурой, а не реальной активацией промокода. 50%: исходные 200 копеек превращаются в 100, retry возвращает тот же расчёт; отзыв через штатную функцию отменяет подготовленный заказ с consent_revoked, резерв released, попытки оплаты нет. 100%: сумма 0, requires_payment=false, период применяется один раз, retry не меняет ревизию, платёжной попытки нет.

После rollback независимое чтение подтвердило прежние revision=4 и period_end, один существующий recurring order, ноль временных кодов и отзывов. HTTP к провайдеру не выполнялся, флаг не включался. Это проверки БД, не полная приёмка UI выпуска/активации промокода и не списание со скидкой у провайдера. Остаются полная цепочка скидок и реальный окончательный отказ с ручным восстановлением. Изменён только журнал; lint/build не запускались.


## Подготовка полной проверки промокода — 23.09.2026

Для отдельной организации создано одно новое предложение актуального Pro на 200 копеек, expected_revision=4, следующий календарный месяц после текущего периода, срок принятия семь дней. Проверка current_offers=1. Текущая подписка, согласие и сохранённый метод не изменялись; заказы и списания не создавались. Следующий пользовательский шаг: штатная акция Pro со скидкой 50% на два месячных периода, выпуск персонального кода через админку с MFA, применение кода и тестовая оплата 100 копеек. Повторное согласие для этого ручного заказа не требуется для проверки скидки; старое действующее согласие не дублировать.

Проверены официальные сведения о sandbox ЮKassa: https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing . Наличие тестовых карт отказа само по себе не подтверждает сценарий успешной привязки с последующим отказом автоплатежа; этот сценарий ещё не подготовлен и не считается пройденным. Изменён только журнал, diff-check PASS; код не менялся.


## Найден дефект ранней покупки следующего периода — 23.09.2026

После пользовательской оплаты скидочного заказа независимое чтение подтвердило succeeded/paid, amount=100 при base=200 и discount_bps=5000, requires_review=false. Но заказ и fulfillment находятся в review/discount_reconciliation_required. Резерв скидки reserved: использовано 0 из 2, зарезервирован 1. Согласие осталось одно, подписка не изменилась (revision=4).

Диагностический вызов fulfill_discount_payment в транзакции с ROLLBACK вернул 22023: confirmed billing period not current. Причина: подготовленное Ромой предложение начинается после текущего оплаченного периода, а non-trial обработчик пытается подтвердить будущий период немедленно. Нельзя считать эту оплату завершённой приёмкой скидки и нельзя повторять платёж.

Следующее исправление: отдельная миграция для безопасного deferred будущего проверенного скидочного платежа; сохранить резерв и прежний доступ, применять в дату начала, проверить конфликт ревизии, повтор, отсутствие повторного списания и автоматическую сверку. Нужны SQL/RLS-регрессия, проверка очереди и интерфейса, контролируемый выпуск, затем повторная обработка уже оплаченного заказа. Docker сейчас остановлен, интеграционные тесты не запускались. Изменён только журнал; исправление ещё не реализовано и stage-код не менялся.


## Исправление будущего скидочного платежа — локально

Миграция 20260923030000 добавляет закрытый helper ожидания подтверждённой оплаты будущего non-trial периода. Проверяются доказательство оплаты, исходная ревизия, active без trial и начало точно на конце текущего периода. До даты возвращается deferred/future_period, сохраняются резерв и очередь sandbox-сверки; на дате работает прежняя проверенная выдача. Конфликт остаётся review. Добавлен отдельный allowlist/workflow future-discount-dry-run/apply.

Регрессия воспроизводит будущую покупку, повтор до даты, конфликт ревизии, закрытые права/чужую организацию, наступление даты и однократную выдачу со списанием льготного периода. Replay 76 миграций с SQL/RLS/concurrency/PostgREST/Edge PASS, 154.76 с. Guard 14 тестов PASS, lint/build PASS с прежними предупреждениями. Stage ещё не обновлён; оплаченный заказ требует повторной сверки после выпуска.


## Выпуск и восстановление скидочного заказа — 23.09.2026

PR #90 объединён после полного CI (10m38s), admin/MFA и CodeQL PASS. Stage SHA 947a3054365c7656ee01a9aa3a4ffb6843e998cf. Dry-run 35854179928 подтвердил только 20260923030000; apply 35854325073 PASS, pending отсутствует. Повторная сверка 35854476540 PASS: checked=1, deferred=1, review=0, failed=0, без новых платежей.

Независимое чтение подтверждает succeeded/paid на 100 копеек и fulfillment=deferred/future_period. Купленный период: 23.10.2026 13:26:10 — 23.11.2026 13:26:10 (Москва). Текущая подписка сохранила revision=4 и окончание 23.10.2026; использовано 0 льготных периодов, зарезервирован 1 из 2. Технический order_state остаётся review до исполнения (существующая семантика успешной оплаты), а статус выдачи больше не требует ручной сверки. Автопродление не включалось. Фактическое применение на будущей дате проверено локальным тестом; на stage дата ещё не наступила. Очередь сохранена; автоматическое расписание этим выпуском не добавлялось.

## Подтверждение оплаты без дубля — 23.09.2026, локально

После пользовательского подтверждения корректного deferred убрано второе общее сообщение об успешной оплате, когда уже показан applied/deferred/review. Сообщения о необходимости проверки платежа и ссылка провайдера сохраняются. Deferred больше не обещает начало периода при payment_requires_review.

Проверки: 27 существующих тестов checkout/consent/runner PASS; после добавления регрессии checkout 14/14 PASS (три сценария повторной проверки applied/deferred/review). Lint PASS с прежними предупреждениями, build PASS, diff-check PASS. Изменены SandboxCheckout.jsx, SandboxCheckout.test.jsx и этот журнал. Исправление пока локальное, на stage не опубликовано.

Оставшаяся приёмка: наступление уже купленного будущего периода на stage, затем автопродление с оставшимся льготным периодом; отдельно 100% скидка и окончательный отказ провайдера с уведомлением/ручной оплатой. До исполнения будущего оплаченного заказа запуск recurring не доказывает эти сценарии. Даты stage и состояние согласия в этой итерации не менялись, списания не запускались.

## Подтверждение без дубля опубликовано — 23.09.2026

PR #91 объединён в staging: 439f3cfa237f489b7a8f6ac790c024dc47218b26. CI 35856958831 PASS (10m26s), CodeQL PASS. Workers Builds: quest-platform-staging success, публичная страница HTTP 200 отдаёт index-CHS4njaE.js и OrganizationBilling-ClevJS01.js. В публичном модуле подтверждено условие подавления повторного succeeded для applied/deferred/review. Проверка публикации по HTTP/модулю не заменяет пользовательский браузерный smoke. Дополнительно 39 локальных worker/flow/batch/notice тестов PASS.

Официальная документация https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing подтверждает отдельный сценарий успешного сохранения тестовой карты и отказа повторной оплаты insufficient_funds. Для приёмки нужна отдельная пользовательская привязка с согласием; использовать тестовую карту из этого раздела документации. Критерии: один отказ, отсутствие повторного списания, уведомление и ручная оплата, снятие предупреждения после нового периода. Этот provider-сценарий пока не выполнен. Текущий оплаченный будущий заказ, даты, согласие и серверный флаг в этом выпуске не изменялись. Production не затрагивался.
## Отдельная приёмка отказа — подготовка 23.09.2026

На stage создана sandbox-recurring-decline (79c66a73-2cf1-4d43-ba03-258d41f303de), выбранный ранее тестовый аккаунт назначен owner через active membership. Операция транзакционная, с проверками уникальности организации и единственного sandbox-магазина. Назначен актуальный Free, создано одно предложение актуального Pro за 200 копеек на календарный месяц, срок принятия 7 дней. Read-back: free/revision=1, offers=1, orders=0. Проверка authenticated в rollback: can_manage=true, visible_offers=1.

Следующее действие владельца: открыть новую организацию в stage, подготовить заказ без промокода, сохранить новое явное согласие до отправки платежа, оплатить специальной тестовой картой из раздела ЮKassa «Неуспешный сценарий при платеже с использованием сохраненного способа оплаты». Первый платёж должен пройти, повторный — получить insufficient_funds. Автоматическое списание, перенос даты и переключение серверной области worker ещё не выполнялись. Существующая sandbox-recurring-acceptance и её будущий оплаченный заказ не изменены. Схема/код не менялись, тесты сборки повторно не требовались.
## Первый платёж сценария отказа — 23.09.2026

После сообщения владельца проверена sandbox-recurring-decline: заказ на 200 копеек finished, fulfillment applied, подписка active/revision=2; период 23.09.2026 15:36:59 — 23.10.2026 15:36:59 (Москва). Есть одно согласие и один подтверждённый сохранённый способ оплаты. Для немедленной проверки следующего автоплатежа требуется отдельно согласовать досрочное завершение именно этого тестового периода; прежнее разрешение касалось другой организации. До этого даты, серверная область и флаг worker не меняются.
## Подтверждённый отказ автопродления — 23.09.2026

По отдельному разрешению Алексея сокращён только первый тестовый период sandbox-recurring-decline с guard revision=2/точная старая дата; новая revision=3, конец 23.09.2026 16:12:18 Москва. Workflow 35865465697 PASS: разовое выполнение на scope этой организации, always-disable и disabled smoke успешны. Независимое чтение: один recurring-заказ, одна попытка/dispatch на 200 копеек, canceled/paid=false/requires_review=false, revision=3 и конец периода не изменены. RPC владельца возвращает payment_failed. Причина insufficient_funds ожидается по специальной карте, но в нашей нормализованной таблице не хранится; подтверждён именно окончательный canceled, без утверждения о прочитанном cancellation_details. Второй запуск списания не выполнялся.

Подготовлено одно новое предложение Pro за 200 копеек для ручного восстановления с expected_revision=3; резервов reserved нет. Следующий шаг владельца: увидеть уведомление, закрыть завершённый первоначальный checkout при необходимости, создать новый заказ без нового согласия и оплатить обычной успешной тестовой картой. После оплаты проверить выдачу периода и снятие payment_failed. Прежняя организация с будущим оплаченным периодом не изменялась. Код/схема не менялись, журнал обновлён.
## Ручное восстановление после отказа подтверждено — 23.09.2026

После оплаты владельцем независимое чтение sandbox-recurring-decline подтвердило новый ручной заказ на 200 копеек: succeeded/paid=true/requires_review=false, finished, fulfillment=applied. Подписка active/revision=4, период 23.09.2026 16:15:11 — 23.10.2026 16:15:11 Москва. RPC read_recurring_failure_notice в контексте владельца возвращает null: предупреждение снято после выдачи нового периода. По-прежнему один recurring-заказ с canceled/paid=false и одно согласие; дополнительных автоматических попыток не появилось.

Сценарий подтверждённого отказа и ручного восстановления принят по данным сервера. Отдельное визуальное подтверждение исчезновения предупреждения не запрашивалось. Worker после workflow 35865465697 выключен, новых запусков в этой проверке нет. Остаются полная stage-приёмка скидочного автопродления 50/100% и отзыва до отправки; они не считаются завершёнными этим результатом. Изменён только журнал; код и схема не менялись.
## Отдельная приёмка скидочного автопродления — 23.09.2026

Создана sandbox-recurring-discount (5e5d15e2-64e2-4f96-bca4-5ff5625bf0cf) с прежним тестовым владельцем, active membership/owner, текущим Free и одним sandbox-предложением актуального Pro за 200 копеек на месяц, срок принятия 7 дней. Подготовка транзакционная, защита от повторного создания. Найдена действующая утверждённая акция «Pro, скидка 50%, два периода по месяцу», активация до 25.09.2026 13:46 Москва.

Следующий шаг владельца: выдать персональный код новой организации по этой акции штатной MFA-формой, применить его к заказу, проверить 100 копеек, сохранить новое согласие до отправки платежа и оплатить успешной тестовой картой. Промокод, согласие и платёж помощником не создавались. Существующий будущий оплаченный период другой организации не меняется. Для последующего ускоренного продления потребуется согласование сокращения только нового тестового периода. Серверная область/флаг worker не менялись.
## Первый льготный платёж подтверждён — 23.09.2026

По сообщению владельца проверена sandbox-recurring-discount: единственный заказ на 100 копеек, discount_bps=5000, succeeded/paid=true/requires_review=false, finished/applied. Подписка active/revision=2, период 23.09.2026 16:26:26 — 23.10.2026 16:26:26 Москва. Из двух льготных периодов один consumed, reserved=0; один действующий сохранённый способ оплаты с неотозванным согласием. Следующее: отдельно согласовать досрочное завершение этого периода и одну попытку автопродления на оставшийся льготный период (ожидается 100 копеек). Даты и worker в этой проверке не менялись, дополнительных списаний нет.
## Автопродление со скидкой 50% подтверждено — 23.09.2026

По отдельному разрешению досрочно завершён только период sandbox-recurring-discount с guard revision=2/точный старый period_end, проверкой единственного действующего метода, отсутствия незавершённых заказов и одного использованного льготного периода. Revision=3, конец 23.09.2026 16:36:11 Москва. Workflow 35868221918 PASS, включая always-disable и проверку отключённого endpoint.

Независимое чтение: одна отправка на 100 копеек, succeeded/paid=true/requires_review=false, резерв consumed, использовано 2 льготных периода. Подписка revision=4, новый период 23.09.2026 16:36:11 — 23.10.2026 16:36:11 Москва. Второе списание не запускалось. Следующие незавершённые сценарии: 100% скидка без провайдера и отзыв согласия до отправки; исчерпание скидки подтверждено счётчиком, фактический следующий платёж по полной цене не выполнялся. Код/схема не менялись, обновлён журнал.
## Подготовка приёмки скидки 100% — 23.09.2026

Проверен штатный путь: сначала успешная выдача периода по 100% коду активирует льготу (consumed), затем reserve_renewal_discount выбирает оставшийся период; zero_amount выдаётся без провайдера. Для повторного использования сохранённого способа подходит sandbox-recurring-discount, где прежние два периода 50% уже исчерпаны. Текущая подписка active/revision=4 до 23.10.2026 16:36:11 Москва. Нулевой ручной checkout не поддерживает выдачу будущего non-trial периода; нельзя готовить ещё один будущий заказ по примеру платного deferred.

План ускоренной проверки: после отдельного разрешения завершить текущий тестовый период, подготовить предложение; владелец выдаёт новый код Pro 100% на два месячных периода и подтверждает нулевой заказ без нового согласия. После подтверждённой выдачи досрочно завершить только этот новый тестовый период и однократно запустить scoped worker; ожидаются второй consumed, новая подписка и отсутствие новых payment attempts/dispatches. Обработчик выключить и проверить. Нужно разрешение на обе перемены дат; существующее разрешение на 50% уже исполнено. Пока подписки/коды/предложения/worker не менялись.
## Разрешение и предложение для 100% — 23.09.2026

Алексей разрешил досрочно завершить два периода sandbox-recurring-discount: текущий и следующий, полученный по новому 100% коду, затем выполнить один scoped-запуск автопродления и выключить worker. Это разрешение сохраняется для второго шага после пользовательского подтверждения нулевого заказа; повторно его не запрашивать.

Первый SQL подготовки отклонён из-за ошибки скобок в timezone; транзакция откатилась. Исправленный атомарный запрос с единым starts завершился успешно: guarded revision=4/точная дата, после изменения revision=5, конец 23.09.2026 16:44:29 Москва, одно новое предложение Pro на месяц с исходной ценой 200 копеек и expected_revision=5. Согласие сохранено, worker не запускался. Следующий пользовательский шаг: создать/утвердить акцию Pro 100% на два месячных периода с текущим окном активации, выдать персональный код этой организации, подтвердить нулевой заказ через кабинет без нового согласия и без ввода карты. Затем прочитать результат и выполнить второй уже разрешённый шаг.
## Бесплатное автопродление 100% подтверждено — 23.09.2026

Пользователь подтвердил нулевой заказ: read-back active/revision=6, один fulfillment, 100% код на два периода, consumed=1/reserved=0. По ранее выданному разрешению выполнено второе сокращение только этого тестового периода (guard revision=6 и точная дата), revision=7, конец 23.09.2026 16:52:13 Москва.

Workflow 35870101142 PASS, always-disable и disabled smoke PASS. Независимое чтение нулевого recurring-заказа: amount_minor=0, attempts=0, dispatches=0, резерв consumed, использовано 2 периода, подписка revision=8 на 23.09.2026 16:52:13 — 23.10.2026 16:52:13 Москва. Бесплатный период выдан без платёжной попытки/отправки; повторный запуск не выполнялся. Разрешение на два сокращения исполнено полностью. Следующее — отзыв согласия до отправки; этот stage-сценарий пока не завершён. Обновлён только журнал, код/схема не менялись.
## Отзыв до отправки — подготовлено 23.09.2026

Штатной закрытой prepare_recurring_order подготовлен будущий заказ sandbox-recurring-discount на конец текущего периода (23.10.2026 16:52:13 Москва, expected_revision=8). Проверен расчёт 200 копеек после исчерпания льгот. Read-back: attempts=0, dispatches=0. Провайдер не вызывался, worker не включался, действующий период не сокращался. Следующий шаг — отзыв существующего согласия владельцем, затем review подготовленного заказа и проверка блокировки отправки/освобождения резерва.

Ограничение текущего интерфейса: RecurringConsent встроен только в карточку исходного sandbox-заказа. Если этот завершённый заказ уже закрыт в браузере, отдельного экрана управления действующим согласием сейчас нет. Не предлагать создавать новое согласие/платёж для отзыва старого; при недоступной кнопке требуется отдельное управление согласием в «Тариф и лимиты». Отзыв ещё не выполнен, сценарий не считается пройденным.
## Управление согласиями вне заказа — реализация

По запросу Алексея добавлен самостоятельный блок «Автопродление» в «Тариф и лимиты». Он перечисляет все согласия организации (pending/saved/revoked), доступен billing.manage независимо от текущего checkout и позволяет штатно отозвать согласие. Новый закрытый RPC list_sandbox_recurring_consents возвращает только ID согласия/заказа, дату и статус; проверяет организационное право, не раскрывает метод оплаты, не требует нахождения в sandbox scope. Выдача нового согласия остаётся в checkout. Отзыв использует существующую команду с аудитом.

Добавлены UI/API/SQL проверки закрытого заказа, отсутствия scope, чужой организации, безопасных полей, двойного клика, ошибки и позднего ответа другой организации. Отдельные операции recurring-consents-dry-run/apply допускают только миграцию 20260923040000 после всех предыдущих. UI/API/guard 38 PASS, дополненная страница 17 PASS; lint/build PASS с прежними предупреждениями. Полный replay 77 миграций с SQL/RLS/concurrency/PostgREST/Edge PASS (161.68s); выпуск пока не произведён.

## Управление согласиями опубликовано — 23.09.2026

PR #92 merged, stage SHA 67b947adf05366f26bcd9e596515e153132ceb15. CI 35873113852 PASS (10m27s), admin frontend/auth и CodeQL PASS. Дополнительная гонка начального чтения после refresh/revoke устранена, 4 компонентных теста PASS; lint/build повторно PASS. Dry-run 35874610733 подтвердил только 20260923040000; apply 35874802819 PASS. Cloudflare Build PASS; публичный OrganizationBilling-DGbXxs_H.js содержит отдельный блок recurring-consent-management и RPC списка. Браузерный визуальный smoke пользователя ещё ожидается. Флаг worker не включался, согласия и подписки выпуском не менялись. Следующее: пользователь отзывает действующее согласие в новом блоке sandbox-recurring-discount, затем проверяем ранее подготовленный будущий заказ без отправки платежа.
### 2026-09-23 — stage: отзыв согласия до отправки принят

Алексей отозвал согласие в отдельном блоке «Автопродление» организации sandbox-recurring-discount. Read-back подтвердил одну запись отзыва. Для подготовленного будущего заказа (expected_revision=8, начало 2026-10-23 13:52:13.590801+00) review_recurring_order вернул cancelled / consent_revoked. Повторная проверка дала тот же результат; резерв released, attempts=0, dispatches=0. Подписка active, revision=8, окончание 2026-10-23 13:52:13.590801+00 сохранено. Worker и провайдер не запускались. Фактическое последующее списание полной цены после исчерпания скидки этим сценарием не проверяется. Результат отражён в Notion.
## 2026-09-23 — разделение состояний проверки в админке (локально)

Первый срез следующего пункта: PaymentReviewStatus отдельно показывает проверку платежа и возврата, включая одновременные флаги. Для order_state=review без payment_requires_review поясняет, что обработка заказа и подтверждение оплаты различаются; конкретная причина из текущего RPC неизвестна и не угадывается. Серверная проекция и полномочия не изменены. Следующий срез: безопасная серверная проекция причины/отложенного исполнения с SQL/RLS проверками; текущая доработка не завершает этот пункт целиком.

Проверки: organizationPayments — 8 PASS; lint PASS с прежними предупреждениями; build приложения PASS. Первая сборка admin остановлена проверкой отсутствующего окружения; повтор с синтетическими параметрами --mode test PASS. Изменения не опубликованы. Файл recurring-visual.local.mjs сохранён без изменений.
## 2026-09-23 — серверная проекция обработки платежа (локально)

Миграция 20260923050000 расширяет существующий read_platform_organization_payments: fulfillment_state, ограниченный список fulfillment_reason, fulfillment_checked_at, period_start. Неизвестные причины заменяются other; диагностические строки не раскрываются. Сохраняются billing.payment.read, MFA, область организации, аудит и пагинация. Чтение не запускает выдачу периода или сверку провайдера.

Админка отделяет deferred от ошибки: «Ожидает активации периода», дата начала в часовом поясе устройства. Проверки платежа и возврата остаются независимыми. Старый API без новых полей поддерживается. Добавлены SQL-проверки проекции и отсутствия утечки неизвестной причины; существующие проверки MFA/чужой организации/отзыва действуют для расширенного ответа. UI: 11 PASS; lint PASS с прежними предупреждениями; приложение и admin (--mode test, синтетическое окружение) build PASS. Первый replay остановился на счётчике 77 вместо 78 после успешного применения миграций; счётчик обновлён. Результат полного повторного replay указан ниже. Stage не изменялся; миграция ещё не включена в разрешённый пакет выпуска.
Повторные интеграционные прогоны: 171.35s и 168.03s — FAIL на verifyRecurringEdge: Test Edge Runtime did not become ready (пустой журнал). Оба прошли применение 78 миграций, SQL/RLS, конкурентную часть и дошли до Edge/PostgREST harness. Полный интеграционный PASS не заявляется. Отдельный docker run --rm supabase/edge-runtime:v1.74.3 --version PASS; причина неготовности HTTP не установлена. До выпуска требуется диагностика интеграционного запуска и успешный полный прогон. Stage/production не изменены.
## 2026-09-23 — диагностика холодного запуска Edge

Отдельный диагностический контейнер без секретов показал: mounts доступны, процесс running/exit=0; debug-журнал останавливается на разрешении npm-зависимостей @supabase/supabase-js, затем загрузка завершается и GET отвечает 405. Образ --version и доступ к registry проверены. Это подтверждает задержку холодного старта, а не ошибку миграции или падение обработчика. Диагностический контейнер удалён; общие контейнеры не останавливались.

В scripts/recurring-edge.mjs прежние 40 попыток с паузой 500 мс заменены ограничением времени 180 секунд; ошибка дополняется статусом и exit code контейнера, маскирование тестовых токенов сохранено. Общий бюджет интеграционного теста увеличен до 420 секунд. Проверки 401/200, выдачи периода и отсутствия повторного исполнения не ослаблены. Один промежуточный прогон ещё использовал загруженный старый цикл и завершился прежней ошибкой; финальный запуск выполнен новым процессом.
Финальный полный прогон нового процесса PASS: 174.89s, 78 миграций, SQL/RLS, concurrency, PostgREST и Edge со штатным обработчиком и синтетическим провайдером. Lint и build PASS (прежние lint warnings), diff-check PASS. Временные контейнеры harness удалены. Блокер локальной интеграционной проверки снят. Изменения проекции/интерфейса и тестового запуска пока локальные; следующий шаг — контролируемый пакет stage-миграции 20260923050000 и выпуск frontend после неё. Production и платежи не изменялись.
## 2026-09-23 — причины обработки опубликованы на stage

PR #93 merged: 7627bb7df695cc0cf20e0209cf3510bc02e21863. CI 35902063210 PASS (9m49s), admin frontend/auth и CodeQL PASS. Dry-run 35903379754 подтвердил только 20260923050000; apply 35903563041 PASS, pending нет. После восстановления доступа проверен завершённый workflow без повторного применения.

Cloudflare Git Build загрузил версию 39824643-14c9-4565-ad81-06686148f102; она активирована на 100% только qvesta-admin-stage. HTTP 200, публичный /assets/index-DKtUkIUR.js содержит fulfillment_reason, ожидание активации периода и отдельную проверку возврата. Это проверка публикации, не пользовательский smoke с MFA. Платёжные операции, подписки и production не менялись. Автоматическая активация будущих Git Builds остаётся отдельным пунктом.
## 2026-09-23 — детали заказа и переход к тарифу (локально)

По запросу Алексея добавлены название и номер версии тарифа, период заказа, исходная стоимость, скидка и сумма, состояние выдачи периода, время проверки платежа. ID скрыты в раскрываемых технических данных. Новая миграция 20260923060000 читает точный plan_version_id заказа и неизменяемые quote ручного/recurring заказа, не текущие цены. MFA, область роли, аудит и пагинация сохранены; quote целиком не возвращается.

Название версии открывает штатную карточку Tariffs внутри раздела организации. Список платежей остаётся смонтированным скрытым, «К платежам» возвращает данные без повторной загрузки и фокус на ссылку. Карточка тарифа отдельно проверяет штатные права каталога через API. 19 UI-тестов PASS, включая точный ID и сохранение списка; lint и обе сборки PASS (admin с синтетическим test-окружением). Полный replay 79 миграций PASS (158.06s): SQL/RLS, конкурентные проверки, PostgREST/Edge. Stage этой доработкой пока не изменён.
## 2026-09-23 — детали заказа опубликованы

PR #94 merged: 2a5aeccd7100f59035fcee13c6ae245b94bffd04. CI 35910870398 PASS (10m40s), admin frontend/auth и CodeQL PASS. Guard: 17 тестов PASS. Dry-run 35912110015 — только 20260923060000; apply 35912257583 PASS, pending нет. Серверный read-back подтвердил новую проекцию plan_number.

Версия Cloudflare 8312cd4b-3ecf-468d-83f7-fd7e8229ddc7 активирована на 100% qvesta-admin-stage. HTTP 200, /assets/index-Clg2pD_D.js содержит детали стоимости, технический блок и возврат к платежам. Пользовательский smoke с MFA ещё не выполнен. Production, платежи и подписки не изменены. Следующее: визуальная приёмка деталей/ссылки и автоматическая активация будущих admin Git Builds.
## Актуальный статус и план для синхронизации с Notion — 23.09.2026

В текущем аккаунте поиск подключения показал Notion installed=false; инструменты чтения/изменения страниц отсутствуют. Предложена установка/авторизация. Notion этим запросом не обновлён; текст ниже подготовлен для переноса после подключения.

Завершено на stage: PR #93 — отдельные состояния проверки платежа, возврата и отложенной активации; PR #94 — детали заказа, сохранённая цена/скидка/период, ссылка на точную версию тарифа и возврат к списку. Обе миграции применены, frontend активирован, CI и проверки публичной сборки успешны. Полный локальный replay 79 миграций с SQL/RLS/concurrency/PostgREST/Edge PASS. Отзыв согласия до отправки ранее принят: подготовленный заказ отменён, резерв освобождён, новых отправок нет, оплаченный доступ сохранён.

Ближайший план:
1. Визуально проверить в MFA-сессии детали платежа, раскрытие ID, переход на версию и возврат к сохранённому списку; до проверки не считать пользовательскую приёмку завершённой.
2. Настроить автоматическую активацию stage-админки после успешного Git Build с проверкой целевого Worker/commit и совместимости миграций; сохранить возможность отката. Сейчас активация ручная.
3. Закрыть оставшиеся проверки автопродления: наступление заранее оплаченного будущего периода на stage и фактическое списание полной цены после исчерпания скидки. Расчёт полной цены уже проверен, реальное списание этим не подтверждено. Не сокращать периоды и не создавать новое согласие без согласованного сценария.
4. Перед коммерческим запуском реализовать согласованное внешнее хранение резервных копий и проверку восстановления. Production в текущий выпуск не входит.
## 2026-09-23 — синхронизация Notion восстановлена

После установки Notion подтверждены чтение и запись страницы «Техническая документация» 3df511103a9a818185fcf575781cd9bb. Статусы PR #93/#94 обновлены на stage опубликован; перенесены результаты CI, миграций и активации. Ближайший план выделен отдельно от выполненных этапов: визуальная приёмка, автоматическая активация admin Git Builds, оставшиеся stage-сценарии будущего периода/полной цены, резервные копии перед коммерческим запуском. Повторное чтение подтвердило изменения; устаревшее ожидание CI PR #93 удалено. Предыдущая отметка о недоступном подключении историческая и более не актуальна.
## 2026-09-23 — приёмка деталей и автоматическая активация admin

Алексей подтвердил успешную визуальную проверку деталей заказа. Пользовательская приёмка PR #94 завершена.

Цель следующего шага: вернуть Deploy command Cloudflare Git Builds для qvesta-admin-stage с versions upload на npx wrangler@4.134.0 deploy --config admin/wrangler.stage.jsonc. Сборка: npm run build -- --config admin/vite.config.mjs --mode staging; ветка staging; non-production branches выключены; root /. Production и другие Workers не изменять. Сборка считается выпущенной только после успешного deployment и HTTP-проверки публичного артефакта. Для отката сохранена текущая версия 8312cd4b-3ecf-468d-83f7-fd7e8229ddc7 (100%).

Порядок будущих релизов: совместимые добавления API допускают публикацию frontend до миграции только при проверенном fallback старого контракта. Для несовместимых изменений сначала отдельный backend PR/миграция, затем frontend PR. Git Build не применяет миграции автоматически и не подтверждает их готовность. При необходимости временно использовать versions upload и активировать версию после проверки БД; не считать upload активацией.

Браузерный инструмент не запустился: windows sandbox failed: helper_unknown_error: apply deny-read ACLs. Настройка через UI не изменена. Алексей получил точную команду и просьбу сохранить её и повторить последнюю сборку. Ожидается подтверждение, затем read-back deployment версии и публичного артефакта. До этого автоматическая активация не считается включённой.
## 2026-09-23 — автоматическая активация stage admin подтверждена

После сообщения Алексея «готово» проверен GitHub check Workers Builds: qvesta-admin-stage — success на 2a5aeccd7100f59035fcee13c6ae245b94bffd04, 20:41:17 UTC. Версия e4fb670b-9b2c-4b83-9301-fd54c5077d9e создана 20:41:11 UTC, deployment 58d9a072-3601-4417-ac08-89d21be0dcb9 в 20:41:12 UTC направил на неё 100% трафика. Рома не запускал versions deploy в этой проверке. Это подтверждает автоматическую активацию повторной Git-сборки после изменения команды пользователем.

HTTP 200, публичный /assets/index-Clg2pD_D.js содержит детали заказа и «К платежам». Артефакт совпадает с предыдущим, поскольку пересобран тот же commit. Предыдущая версия для отката: 8312cd4b-3ecf-468d-83f7-fd7e8229ddc7. Статус в Notion обновлён на завершённый. Production, БД, платежи и подписки не менялись.
## 2026-09-23 — подготовлен отдельный финальный stage-сценарий

Алексей согласовал отдельную организацию с коротким начальным периодом, покупкой будущего периода и последующей проверкой полной цены. Создана sandbox-recurring-final (3a20ba48-2c91-41c5-8c30-506eba97a944), прежний тестовый аккаунт назначен owner через active membership. Начальное состояние active/revision=1 задано как техническая sandbox-фикстура, не как доказательство оплаты; платежи и подтверждения для него не создавались. Начальный период заканчивается 24.09.2026 23:57:47 Москва.

Одно предложение актуального Pro: 200 копеек, следующий календарный месяц с 24.09.2026 23:57:47 до 24.10.2026 23:57:47 Москва; принять до начала периода. Транзакция защищена от повторного создания; read-back offers=1/orders=0. Проверка authenticated в rollback: billing.manage=true, visible_offers=1. В область sandbox application добавлена только новая организация; recurring worker не включался, согласия/методы не создавались. Старые организации и заказы не менялись.

Следующий шаг владельца: в stage-админке выдать этой организации персональный код по существующей утверждённой акции «Pro 50 % 1-1» (50%, один период по месяцу). В кабинете применить код, проверить сумму 100 копеек, создать заказ, сохранить новое согласие на автопродление и оплатить успешной тестовой картой. Затем проверить succeeded + deferred до даты; после даты выполнить штатную сверку, проверить applied и однократное расходование льготы. Само расписание сверки этим шагом не создаётся. После активации подготовить проверку полной цены 200 копеек; будущий месячный период не сокращён, для ускорения его окончания требуется отдельная конкретизация сценария. Не считать полную цену фактически списанной до результата провайдера.
## 2026-09-24 — новый будущий период оплачен, ожидание подтверждено

После сообщения Алексея «готово» независимое чтение stage подтвердило заказ sandbox-recurring-final: 100 копеек, succeeded/paid=true/requires_review=false, fulfillment=deferred/future_period. Начало 24.09.2026 23:57:47 Москва, окончание 24.10.2026 23:57:47. Технический order_state=review при deferred не означает отказ оплаты. Текущая подписка сохранила active/revision=1 и исходную дату окончания. Есть одно неотозванное согласие и один подтверждённый сохранённый способ оплаты (идентификаторы метода не извлекались); скидка reserved, ещё не consumed. Проверка выполнена около 24.09.2026 00:09 Москва.

Проверка ранней оплаты пройдена. Выдача на дате и полная цена ещё не проверены. Следующее действие после наступления даты — штатная сверка этого оплаченного заказа с проверкой applied, однократного изменения ревизии и consumed; до даты никаких изменений периода. Фоновое расписание не создавалось; эта запись не обещает автоматическое пробуждение помощника. Провайдер и recurring worker в текущей проверке не запускались.

## 2026-09-24 — ускоренный отдельный сценарий

По запросу нового заказа с минимальным ожиданием подготовлена sandbox-future-fast (3dcb9b80-5920-4fd9-8642-a1ee0803093d) с прежним тестовым владельцем. Технический начальный период 10 минут, revision=1; одно предложение Pro 200 копеек без промокода, начало 24.09.2026 00:25:51 Москва, окончание через календарный месяц. Срок принятия — до начала. Заказ создаётся владельцем через UI; read-back orders=0. Согласие для проверки будущей активации не требуется. Старый оплаченный заказ и его даты сохранены. Worker не включался, фиктивные доказательства оплаты не создавались.


## 2026-09-24 — ускоренная активация будущего периода проверена на stage

Заказ sandbox-future-fast 492a538e-b621-4058-a1e7-d4c78a347256: 200 копеек, succeeded/paid=true/requires_review=false; до старта подтверждён deferred/future_period. После 00:25:51 Москва вызван штатный process_billing_confirmation для этого заказа: applied. Повторный вызов: applied. Независимое чтение: subscription active/revision=2 (была 1), период 24.09.2026 00:25:51 — 24.10.2026 00:25:51 Москва, order finished, одно подтверждение и один заказ. Новые платежи не отправлялись. Это проверка серверной выдачи по наступившей дате и идемпотентности, не автоматического расписания и не списания полной цены после скидки. Старый заказ sandbox-recurring-final сохранён. Изменён только журнал; diff-check PASS.


## 2026-09-24 — общая сверка заблокирована, обнаружена устаревшая проекция

Общий workflow operation=reconcile не запущен: automatic approval review дважды отклонил область всей очереди платежей/возвратов, включая попытку после согласия продолжить. Обход не выполнялся. Read-only проверка заказа 492a538e-b621-4058-a1e7-d4c78a347256: оплата succeeded/paid, order finished, subscription active/revision=2; billing_sandbox_fulfillments пока deferred/future_period. Прямой process_billing_confirmation подтвердил выдачу, но не обновил проекцию, обновляемую обработчиком payment event. Поэтому полную проверку пользовательского статуса нельзя считать закрытой.

Следующий технический шаг: отдельный режим сверки конкретного order_id с проверкой sandbox-магазина и серверной авторизацией, без batch-claim других заказов и без обхода очереди возвратов; тесты отказа чужого заказа, отсутствия массовой обработки и обновления проекции. Выпуск и запуск конкретного режима отдельно от отклонённого общего workflow. Автоматическое расписание и фактическая полная цена после скидки остаются незавершёнными.


## 2026-09-24 — точечная сверка реализована локально

Добавлен отдельный sandbox-reconcile-order: POST с серверным worker-token и обязательным x-qvesta-order-id; ID проверяется до запросов БД, read_sandbox_reconciliation_order проверяет магазин, состояние перечитывается у провайдера и передаётся штатному apply_sandbox_payment_event. Нет claim общей очереди и обработки возвратов. Отсутствующий/чужой заказ не вызывает провайдера; неверный ID не переключает на batch. Runner --order требует SANDBOX_ORDER_ID и фиксирует stage endpoint; workflow operation=reconcile-order разрешён только staging. Публикация новой функции включена в существующий deploy_sandbox шаг. Схема БД и RLS не менялись.

52 unit-теста PASS (handler авторизация, single-order, batch regression, runner). Lint PASS с прежними предупреждениями, build PASS, diff-check PASS. Deno endpoint и полный путь на stage ещё не проверены; изменения не опубликованы. Следующее: выпуск отдельной функции и запуск reconcile-order для 492a538e-b621-4058-a1e7-d4c78a347256, затем read-back billing_sandbox_fulfillments=applied. Отклонённый общий workflow не запускался.


## 2026-09-24 — точечная сверка выпущена и проверена

По явному разрешению выполнены commit/push/PR #95 и squash merge в staging: 4a9cb999f3550cbe02b3925139bd4dc522d32427. CI 35954934388 PASS (9m54s), CodeQL PASS. После проверки соответствия исходников stage опубликован только sandbox-reconcile-order; миграции и секреты не менялись. Публичный smoke: GET 405, POST без токена 401, пустые ответы/no-store.

Workflow 35955735796 с operation=reconcile-order и единственным заказом 492a538e-b621-4058-a1e7-d4c78a347256 PASS: checked=1/state=applied. Независимое чтение: fulfillment applied/reason null, order finished, payment succeeded/paid, subscription active/revision=2; один заказ/одно подтверждение, даты сохранены. Новых платежей и обработки возвратов нет. Устаревшая проекция deferred исправлена штатной сверкой провайдера. Notion OPS-01 обновлён: будущая активация завершена, расписание и фактическое автосписание полной цены после скидки остаются открытыми. Production не затронут.


## 2026-09-24 — локальная подготовка ограниченного расписания

Stage read-only: pg_cron установлен, quest-billing-lifecycle active=false; pg_net отсутствует, Vault пуст. GitHub default branch main, поэтому расписание на staging само по себе не запускается. Выбран отдельный Supabase Cron → pg_net → sandbox-reconcile-order, по официальной документации https://supabase.com/docs/guides/functions/schedule-functions .

Черновик миграции 20260924010000 добавляет закрытый RLS allowlist заказов, срок до 24 часов, максимум 6 попыток с интервалом 5 минут и остановку applied/review/expired/ineligible. До начала оплаченного периода вызов не отправляется. Endpoint фиксирован на stage, token читается из Vault только для готового заказа. Новое расписание создаётся disabled, старое не включается; token и заказы миграцией не создаются. SQL-тесты доступа и остановок добавлены, harness обновлён на 80 миграций.

Lint PASS (прежние предупреждения). Vite build собрал assets, но процесс ещё не завершил PWA-этап; полный build PASS пока не подтверждён. SQL/RLS/replay НЕ запущены: Docker Engine недоступен (pipe dockerDesktopLinuxEngine отсутствует). Алексей получил просьбу запустить Docker Desktop. Миграция не применена и не опубликована; перед выпуском нужен полный SQL-прогон, расширение тестов очереди HTTP/ограничения попыток, настройка Vault без вывода секретов и явное включение только согласованного заказа. Автоматическое расписание пока не работает.

PWA-этап затем завершился, npm run build exit=0: полный build PASS подтверждён.


## 2026-09-24 — проверка и защита расписания

Docker Desktop доступен. Первые replay остановились на настройках изолированного контейнера: preload pg_net, затем собственный preload/getkey_script supabase_vault. Harness теперь создаёт отдельный ключ во временном контейнере; реальные секреты не читаются. SQL-проверки подтвердили постановку точного заказа в HTTP-очередь, отсутствие немедленного повтора, остановку по лимиту и review, отказ без Vault-токена. HTTP-очередь теста откатывается транзакцией без отправки.

Обнаружено стандартное право authenticated на net.http_request_queue. В миграцию добавлен REVOKE для public/anon/authenticated/service_role: заголовки с worker-токеном не должны быть доступны клиенту. Регрессионный тест чтения очереди добавлен вместе с проверкой запрета чтения Vault. Финальный replay выполняется на новой миграции; предыдущий запуск видел старую миграцию и новый тест и корректно выявил открытый доступ.

Подготовлен order-schedule-dry-run/apply в stage workflow и отдельный allowlist только 20260924010000 после полностью применённых предшественников; 18 guard-тестов PASS. Stage read-only подтвердил pg_net в shared_preload_libraries. Релиз создаёт только выключенное расписание; настройка Vault, регистрация заказа и включение выполняются отдельно после проверки.

Финальный полный replay PASS 154.49s: 80 миграций, 28 проверок расписания (включая запрет чтения HTTP-заголовков), остальные SQL/RLS, concurrency, PostgREST/Edge. Временные контейнеры удалены. Guard 18 PASS; lint/build PASS с прежними warnings, diff-check PASS. Подготовлен выпуск выключенного основания без token/заказов/включения cron.


## 2026-09-24 — foundation на stage и корректировка hosted ACL

PR #96 merged, stage a03d86deb486406107309abe56e7356205ef3dc3. CI 35961440204 PASS, admin/auth и CodeQL PASS. Dry-run 35962451728 — только 20260924010000; apply 35962558918 PASS, pending нет. Read-back: оба cron jobs inactive, allowlist пуст, Vault пуст.

Hosted pg_net принадлежит supabase_admin и сохраняет PUBLIC SELECT на HTTP-очередь даже после миграционного REVOKE; postgres не наследует эту роль. Клиентские роли имеют USAGE net. Поэтому локальный запрет чтения не является достаточной защитой stage, token/заказы не настроены и cron не включён. Утечки токенов не было: запросы с токеном не создавались.

Подготовлена отдельная корректировка 20260924020000: очередь содержит HMAC-SHA256 подпись конкретного order_id и timestamp с доменным префиксом; постоянный токен остаётся только в Vault/Edge env. Обработчик разрешает возраст подписи до 90 секунд с допуском будущих часов 5 секунд, отвергает подмену ID/времени и смешанную авторизацию. Повтор подписи может только повторить идемпотентную сверку того же заказа в коротком окне; новая оплата/возврат не создаётся. Ручной bearer-header workflow сохранён. 25 auth-тестов, 19 guard-тестов, lint/build PASS; полный replay 81 миграции выполняется.

Полный replay 81 миграции PASS 152.39s, включая SQL/RLS, 30 проверок расписания, concurrency/PostgREST/Edge; diff-check PASS. Корректировка готова к отдельному stage-выпуску с выключенным расписанием.


## 2026-09-24 — подписанные запросы расписания опубликованы

PR #97 merged, stage 289dc8a3a45e57f835b6c7ba52758003cce69b4a. CI 35963364021 PASS (10m25s), CodeQL/admin frontend PASS. Admin auth первый запуск не прошёл вход по паролю; повтор failed job 35963364076 PASS (2m43s) на том же коде, первопричина не установлена. Опубликован только sandbox-reconcile-order. HTTP GET=405, POST без авторизации=401, неверная подпись=401, no-store.

Dry-run 35964263898 подтвердил только 20260924020000; apply 35964394957 PASS. Read-back функции: signed_headers=true, permanent_token_header=false. Оба cron jobs inactive, allowlist=0, Vault=0. Автоматическая сверка ещё НЕ включена. Далее: настроить Vault без раскрытия worker-токена, проверить подписанный запрос от БД до Edge и включить только согласованный заказ с ограниченным окном; отдельно остаётся фактическая полная цена после скидки. Новые оплаты и возвраты не запускались, production не затронут.

## 2026-09-24 — защищённая настройка токена планировщика

Добавлена stage-only операция order-vault-configure. Токен передаётся из защищённого GitHub environment в временный SQL-файл с правами 0600, результат CLI и сырые ошибки не выводятся, файл удаляется в finally. Проверяются точный project/ref, отключение обоих cron jobs, отсутствие активных заказов расписания и подписанная версия функции. Существующий совпадающий секрет допускает повтор; другой секрет не заменяется. Операция не включает расписание и не создаёт оплат/возвратов.

Read-only stage: согласованный будущий заказ оплачен, review=false, fulfillment=deferred; начало 24.09.2026 23:57:47 МСК. Vault/allowlist пусты, оба cron inactive. Unit 7 PASS. Настройка и подписанный положительный probe ещё не выполнены.
Реальный изолированный replay 81 миграции PASS 160.13s с созданием Vault, повтором и отказами при другом токене/активном cron. Stage ACL read-only: anon/authenticated не читают decrypted_secrets; проверка добавлена в runner. Unit 7 PASS, lint/build PASS.

## 2026-09-24 — Vault настроен, один будущий заказ включён в расписание

PR #98 merged: stage d8ff240a63ac6a244a52188da7f32b1c3c02f7dd. Финальный replay 81 миграции PASS 158.35s, unit 7 PASS, lint/build PASS; CI 35967211833 PASS 9m44s, CodeQL PASS. Защищённый workflow order-vault-configure 35968314089 PASS. Значение токена не выводилось и не сохранялось в репозиторий.

Положительный DB → pg_net → Edge HMAC probe для ранее обработанного заказа: request_id=1, HTTP 200, checked=1/state=applied, timed_out=false. Read-back: одно подтверждение, revision=2 — повтор не создал дубликат.

Включён только quest-stage-order-reconciliation для ранее оплаченного заказа sandbox-recurring-final. Начало 24.09.2026 23:57:47 МСК, первая минутная сверка ожидается около 23:58. Окно до 25.09.2026 00:57:47 МСК, максимум 6 отправок с интервалом не менее 5 минут; строка выключается при applied/review/expiry/attempt limit. Исходные оплаченные даты не изменены. Общий quest-billing-lifecycle выключен; новый платёж/возврат не создаётся.

Независимый read-back в 10:16 МСК: два успешных запуска cron; ровно одна разрешённая строка, attempts=0, fulfillment=deferred до начала периода. Автоматическая активация после даты и остановка строки ещё ожидаются — этот приёмочный пункт не закрыт. Автосписание полной цены после скидки остаётся отдельным сценарием.

Операционный откат при необходимости: выключить quest-stage-order-reconciliation через cron.alter_job и enabled этой единственной строки; оплаченный заказ и его даты сохранять. Production не затронут.
## 2026-09-24 — отображение активации в карточке платежа (локально)

В PaymentReviewStatus выделен блок «Активация периода»: ожидание, выданный период и обработка, требующая проверки, имеют различимое оформление. Используются существующие серверные fulfillment_state/fulfillment_checked_at; подтверждённый оплаченный заказ показывает «Оплачено, ожидает активации», запланированную дату и время последней сверки. Прошедшая дата не считается подтверждением активации. При неизвестной дате или старом ответе отображается явный fallback, предупреждение возврата остаётся отдельным. Дубли статуса из PaymentOrderDetails удалены.

15 UI/API тестов PASS, lint PASS с прежними предупреждениями, основная сборка PASS. Обычная сборка admin отклонена валидатором среды; проверочная сборка с синтетическими параметрами --mode test PASS. Этот артефакт не предназначен для публикации. Изменения пока локальны, stage UI не обновлялся; расписание и БД не менялись. Изменены PaymentReviewStatus.jsx, PaymentOrderDetails.jsx, style.css, organizationPayments.test.jsx и этот журнал.
## 2026-09-24 — блок активации опубликован на stage

PR #99 merged, stage 14fa587495dc22b0a46607918c38929cbf0e741a. Общий CI 35972324444 PASS 10m29s; Admin CI 35972324411: frontend PASS 52s, реальный Auth/TOTP PASS 1m37s, CodeQL PASS. Cloudflare Workers Builds qvesta-admin-stage и quest-platform-staging success.

Независимая HTTP-проверка stage-admin: новый /assets/index-CLZdKIiM.js содержит «Оплачено, ожидает активации» и «Последняя сверка активации»; /assets/index-B1jwvnpo.css содержит оформление payment-activation--applied. Проверка публикации по публичным файлам выполнена; ручной визуальный просмотр под учётной записью не выполнялся. Открывать: карточка организации → Тарифы и оплата → Загрузить платежи. БД, оплаченные даты и вечернее расписание не изменены. Следующая приёмка — автоматическая активация после 23:57:47 МСК и остановка строки расписания.
## 2026-09-24 — подготовка приёмки полной цены

Добавлен OPS-01.4-full-price-acceptance.md: порядок серверных проверок, исходная полная цена 200 копеек после одного периода 50%, действующее согласие, разовый scoped workflow, итог провайдера и идемпотентность. Штатное продление sandbox-recurring-final наступит 24.10.2026 23:57:47 МСК; ускорение требует отдельно согласованного изменения тестового периода. Вечерний заказ не менялся.

В billing_recurring_order.test.sql добавлены 7 проверок именно исчерпанной скидки при подготовке автопродления. Полный изолированный replay 81 миграции PASS 156.75s, 15 runner/disabled тестов PASS, lint/build PASS. Notion обновлён. Фактическое списание полной цены остаётся непроверенным. Изменения теста и новый план локальны; stage/production не менялись.
## 2026-09-25 — полная цена после скидки: sandbox PASS

Автоматическая активация предыдущего оплаченного заказа подтверждена: 24.09.2026 23:58:02 МСК, applied, attempts=1, confirmations=1, строка расписания выключена со stop_reason=applied.

После отдельного подтверждения Алексея ускорен только текущий период sandbox-recurring-final; исторический заказ сохранён. Полная цена 2 ₽ после единственного использованного льготного периода оплачена автоматически. Workflow 36064023904 success, worker отключён и disabled проверен. Read-back: succeeded/paid, review=false, одна попытка/отправка/подтверждение, исходная версия тарифа, revision 3→4. Повтор применения результата — applied без повторной выдачи. Подробности: OPS-01.4-full-price-acceptance.md. Резервное копирование отложено по просьбе пользователя, не завершено.

## 2026-09-25 — актуализация очереди

Notion OPS-01 обновлён: автоматическая активация и полная цена после скидки приняты на stage. Следующее: контроль повтора worker без нового списания; аудит оставшихся критериев отказа/неизвестного результата/отзыва/поддержки версии; ревью локального пакета тестов и документов. Исторические статусы не удалены, помечены как история. BACKUP-01 отложен владельцем, production не включён.

## 2026-09-25 — повтор worker без дублей

Scoped workflow 36065121399 success; после повтора сохранились один заказ/отправка/подтверждение, revision=4 и прежние даты. Новых резервов нет. Worker отключён, Verify disabled state PASS. Контроль повторного запуска закрыт; следующим остаётся аудит приёмочной матрицы. В истории уже подтверждены окончательный отказ и ручное восстановление 23.09.2026; повторять их без причины не требуется.

## 2026-09-25 — аудит критериев

Матрица OPS-01.4-acceptance-matrix.md отделяет принятую stage/sandbox-приёмку от локальных проверок. Остались неподтверждённые на stage сценарии окончания поддержки версии и восстановления неизвестного результата; дефектов этим фактом не установлено. Следующий безопасный срез — ограниченная транзакционная проверка окончания поддержки с откатом, без платежа.

## 2026-09-25 — запрет продления после поддержки: stage с откатом

Пройдены граница срока, запрет подготовки, отмена подготовленного заказа support_ended, освобождение резерва, запрет попытки и идемпотентный повтор. Все временные изменения откачены с сопоставлением исходных строк подписки/хронологии и количества заказов. Платежи не отправлялись. Это приёмка серверной логики на stage, не изменение реальной даты поддержки и не проверка UI её назначения. Детали в OPS-01.4-acceptance-matrix.md.

## 2026-09-25 — интеграционное восстановление неизвестного результата

Расширены scripts/recurring-edge.mjs и scripts/fixtures/recurring-edge/index.ts: потеря ответа после создания синтетического платежа, сохранение резерва без выдачи, backoff, GET-восстановление и один период без нового POST. Полный изолированный replay PASS 171.32 с, lint/build PASS. Код рабочего обработчика, stage и провайдер не менялись. Уровень доказательства — изолированные БД/PostgREST/Edge и тестовый провайдер; реальный сбой на stage не заявляется.

## Итоговое ревью приёмочного пакета — 25.09.2026

Ревью тестов полной цены и восстановления после потери ответа завершено без блокирующих замечаний. Пакет ограничен тестами и документацией, производственные обработчики и схема БД не менялись. Полный replay ранее PASS (171.32 с); результаты stage и изолированной среды разделены в матрице. Актуальная очередь перенесена в OPS-01.4-acceptance-matrix.md. Commit/push не выполнялись; коммерческая готовность и BACKUP-01 не закрыты.
