# OPS-01.4 — матрица приёмки

Аудит 25.09.2026. Основания: OPS-01.4-release.md, OPS-01.4-full-price-acceptance.md, существующие тесты. Исторические локальные результаты не выдаются за реальную проверку провайдера.

| Сценарий | Подтверждение | Статус |
| --- | --- | --- |
| Продление сохранённым способом | Stage workflows 35848808095 и 35849013594, один платёж/период и повтор без дублей | Принят sandbox |
| Скидка 50% | Stage 35868221918, 100 копеек, succeeded, consumed | Принят sandbox |
| Скидка 100% | Stage 35870101142, сумма 0, attempts=0, dispatches=0 | Принят stage без платежа |
| Окончательный отказ и ручная оплата | Stage 35865465697, canceled без выдачи; ручная оплата и снятие уведомления подтверждены в журнале 23.09 | Принят по серверным данным; отдельная визуальная проверка снятия уведомления не зафиксирована |
| Отзыв согласия до отправки | Отзыв владельцем 23.09; cancelled/consent_revoked, released, attempts=0, dispatches=0 | Принят stage |
| Автоматическая активация будущего периода | 24.09 23:58:02 МСК, applied, одна попытка/подтверждение, строка расписания выключена | Принят stage |
| Полная цена после скидки | 36064023904: 200 копеек, succeeded, исходная версия, одно подтверждение | Принят sandbox |
| Повтор всего worker после полной цены | 36065121399: processed=0, счётчики и revision=4 сохранены, worker отключён | Принят stage |
| Потеря ответа и неизвестный результат | sandboxRecurring.test.js / sandboxRecurringFlow.test.js: GET после потери POST, отсутствие повторного POST; billing_recurring_close.test.sql: неизвестный результат не закрывается по возрасту | Изолированные PostgreSQL/PostgREST/Edge Runtime: потеря ответа и восстановление GET проверены 25.09.2026; реальный сбой провайдера на stage не имитировался |
| Окончание поддержки версии | billing_recurring_order.test.sql и billing_recurring_review.test.sql: запрет на границе и после подготовки | Проверка серверной логики на stage с откатом 25.09.2026 PASS; без реального платежа и изменения опубликованной поддержки |

## Актуальная очередь — 25.09.2026

Проверки повторного worker, окончания поддержки и восстановления после потери ответа завершены; подробности и границы доказательств приведены ниже и в матрице приёмки. Итоговое ревью тестового пакета завершено без блокирующих замечаний. Изменены только тесты и документация; производственная логика и схема БД не менялись.

1. Подготовленный пакет тестов и документации готов к фиксации и передаче на ревью; commit/push в рамках этой проверки не выполнялись.
2. Отдельно остаётся визуальная проверка исчезновения уведомления об отказе после ручной оплаты: серверный результат подтверждён, визуальная приёмка не зафиксирована.
3. Постоянные автосписания и production не включены. BACKUP-01 отложен владельцем; реальное резервирование и полное восстановление остаются условием до коммерческого запуска.

BACKUP-01 отложен владельцем и остаётся условием до коммерческого запуска. Новые платежи этим аудитом не запускались.

## Окончание поддержки — проверено 25.09.2026

На stage в ограниченной транзакции (statement_timeout=20s, lock_timeout=3s) использована тестовая sandbox-recurring-final, revision=4. Срок поддержки временно совпадал с началом следующего периода: за микросекунду до границы renewal разрешён, на границе запрещён. prepare_recurring_order вернул ожидаемый recurring support ended. После временной подготовки заказа изменение срока вызвало cancelled/support_ended, резерв released; begin_recurring_attempt вернул cancelled, попыток и dispatch нет. Повтор review сохранил результат.

Вложенная транзакция откачена; внутри проверки сопоставлены полные исходные строки подписки и хронологии, число заказов и отсутствие временного резерва. Затем выполнен внешний ROLLBACK. Проверка относится к серверному ограничению продления, не к пользовательскому назначению даты поддержки через MFA и не к реальному ожиданию календарной даты. Провайдер не вызывался. Следующий незавершённый пункт — управляемое восстановление неизвестного результата в изолированной среде и фиксация границ stage-приёмки.

## Потеря ответа — интеграционная проверка 25.09.2026

Тестовый провайдер сохраняет успешный платёж, затем теряет ответ первого POST. Настоящий обработчик Edge Runtime работает с изолированными PostgreSQL и PostgREST. Проверены: reconciliationRequired=1, отсутствие результата в БД и выдачи периода, сохранённый резерв; немедленный повтор processed=0 из-за backoff; после переноса только тестового next_check_at — поиск платежа GET, succeeded, один dispatch, одно подтверждение, revision=2. Следующий проход processed=0, revision не меняется. Повторный POST тестовый провайдер отвергает. Production-обработчик не изменён; сбой включается только в тестовой фикстуре.

Полный replay scripts/tariff-release-migrations.test.js с QVESTA_TEST_TARIFF_RELEASE=1 прошёл за 171.32 с, включая SQL/RLS, конкурентные проверки, PostgREST и Edge. Lint/build PASS с прежними предупреждениями. Это воспроизводимая интеграционная проверка, не реальная потеря ответа ЮKassa на stage.

Следующее: итоговое ревью локального тестового пакета и актуализация Notion с указанными границами доказательств. Постоянные автосписания и production не включать автоматически; BACKUP-01 остаётся отложенным.

## Уведомление после ручной оплаты — браузерная проверка 25.09.2026

Расширен e2e/sandbox-checkout.spec.js: предупреждение видно до оплаты, ссылка ведёт к ручному оформлению, при pending предупреждение сохраняется, после серверного succeeded/applied и перезагрузки предупреждение и ссылка отсутствуют. Три проекта Playwright PASS (27.6 с), lint/build PASS с прежними предупреждениями. Ответы API синтетические; новые платежи не создавались. Мобильные проекты работают в Chromium и не подтверждают реальный Safari. Снимки сохранены тестом, ручной просмотр снимков заблокирован локальной ошибкой sandbox. Это автоматическая браузерная проверка; ручная визуальная приёмка на stage отдельно не проведена.
